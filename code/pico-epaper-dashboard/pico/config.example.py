# 配置示例 — 复制为 config.py 并填入实际值
# Configuration example — copy to config.py and fill in your values

WIFI_SSID = "YOUR_WIFI_SSID"
WIFI_PASS = "YOUR_WIFI_PASSWORD"

# 服务端地址 / Server address
API_HOST = "YOUR_SERVER_IP"
API_PORT = 80

# DeepSeek API Key (放在服务端 api.php 中, 不在此处)
# DeepSeek API Key (goes in server/api.php, not here)

# 刷新间隔(秒) / Refresh interval (seconds)
INTERVAL = 300
